<UserControl x:Class="ProjectsNow.Views.LibraryViews.SelectItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:ProjectsNow.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Width="auto"
             Height="530"
             Background="{StaticResource GN.Gray}"
             BorderBrush="{StaticResource GN.Blue}"
             BorderThickness="2"
             Style="{StaticResource ViewStyle}"
             mc:Ignorable="d">
    <UserControl.Resources>

        <!--<ContextMenu x:Key="GridContextMenu">

            <MenuItem Command="{Binding RefreshCommand}" Style="{StaticResource GN.MenuItemStyle}">
                <MenuItem.Header>
                    <TextBlock Text="Refresh" />
                </MenuItem.Header>
                <MenuItem.Icon>
                    <Image Source="{StaticResource Reload}" />
                </MenuItem.Icon>
            </MenuItem>

        </ContextMenu>-->

        <!--<ContextMenu x:Key="GroupContextMenu">

            <MenuItem Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGrid}}, Path=DataContext.CopyItemCodeCommand}"
                      CommandParameter="{Binding}"
                      Style="{StaticResource GN.MenuItemStyle}">
                <MenuItem.Header>
                    <TextBlock Text="Copy Item Code" />
                </MenuItem.Header>
                <MenuItem.Icon>
                    <Image Source="/Images/Icons/Copy.png" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem  Style="{StaticResource GN.MenuItemStyle}" Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGrid}}, Path=DataContext.CopyAllCommand}">
                <MenuItem.Header>
                    <TextBlock Text="Copy Sheet" />
                </MenuItem.Header>
                <MenuItem.Icon>
                    <Image Source="{StaticResource CopySheet}" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem  Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGrid}}, Path=DataContext.RefreshCommand}" Style="{StaticResource GN.MenuItemStyle}">
                <MenuItem.Header>
                    <TextBlock Text="Refresh" />
                </MenuItem.Header>
                <MenuItem.Icon>
                    <Image Source="{StaticResource Reload}" />
                </MenuItem.Icon>
            </MenuItem>

        </ContextMenu>-->

        <Style x:Key="PropertyGrid" TargetType="Grid">
            <Setter Property="Height" Value="30" />
        </Style>

        <Style x:Key="PropertyTextBlock" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Times New Roman" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Margin" Value="5,2,0,2" />
            <Setter Property="Width" Value="110" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="PropertyImage" TargetType="Image">
            <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Width" Value="20" />
            <Setter Property="Margin" Value="5,0,5,0" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="PropertyListBox" TargetType="ListBox">
            <Setter Property="Height" Value="110" />
            <Setter Property="Width" Value="150" />
            <Setter Property="BorderBrush" Value="White" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="Times New Roman" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ItemContainerStyle" Value="{DynamicResource ListBoxItemStyle}" />
        </Style>

        <Style x:Key="ListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Name="_Border"
                                Padding="2"
                                SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="_Border" Property="Background" Value="LightGray" />
                                <Setter Property="Foreground" Value="Red" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PropertyBorder" TargetType="Border">
            <Setter Property="Margin" Value="20,10" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Height" Value="155" />
            <Setter Property="Width" Value="170" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Background" Value="{StaticResource GN.Gray}" />
            <Setter Property="Effect" Value="{StaticResource GN.Shadow}" />
        </Style>

    </UserControl.Resources>
    <Grid Background="{StaticResource GN.Gray}">

        <c:PopupControls />

        <Grid Margin="0,0,0,10" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Margin="10,0"
                       VerticalAlignment="Center"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{StaticResource GN.Blue}"
                       Text="Qty" />

            <TextBox Grid.Column="1"
                     Width="60"
                     VerticalAlignment="Center"
                     Style="{StaticResource TextBoxStyle}"
                     Text="{Binding Qty}"
                     TextAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="PreviewKeyDown">
                        <i:InvokeCommandAction Command="{Binding InputIntCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </TextBox>


            <Button Grid.Column="5"
                    Width="100"
                    Height="30"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource CancelButton}" />

            <Button Grid.Column="4"
                    Width="100"
                    Height="30"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource GN.AcceptButton}" />

            <Button Grid.Column="3"
                    Width="100"
                    Height="30"
                    Command="{Binding ResetSelectionsCommand}"
                    RenderTransformOrigin="-0.208,0.784"
                    Style="{StaticResource GN.ResetButton}" />

        </Grid>

        <Grid Height="100"
              VerticalAlignment="Top"
              Background="{StaticResource GN.Gray}" />

        <StackPanel Margin="5"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Orientation="Horizontal">

            <Image Width="60"
                   Height="60"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Source="{Binding GroupImage}" />

            <TextBlock VerticalAlignment="Center"
                       FontSize="20"
                       Style="{StaticResource TitleStyle}"
                       Text="{Binding Description}" />

        </StackPanel>

        <TabControl Height="400"
                    Margin="0,70,0,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                    Effect="{StaticResource GN.Shadow}"
                    Style="{StaticResource GN.TabStyle}">
            <TabControl.Resources>
                <Style BasedOn="{StaticResource GN.TextTabItemStyle}" TargetType="TabItem" />
            </TabControl.Resources>

            <TabItem Width="20"
                     Header="TabItem"
                     Visibility="Hidden" />

            <TabItem Header="{Binding Categories[0].Id}" IsSelected="True">
                <TabItem.Resources>
                    <Style BasedOn="{StaticResource GN.TextTabItemStyle}" TargetType="TabItem">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Categories[0]}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TabItem.Resources>

                <Grid Margin="10,0"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">

                    <WrapPanel Grid.Row="0"
                               Width="{Binding Categories[0].Width}"
                               HorizontalAlignment="Center"
                               Orientation="Horizontal">

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[0].Properties[0]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[0].Properties[0].Id}" />
                                <Image Source="{Binding Categories[0].Properties[0].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[0].Properties[0].Values}"
                                         SelectedValue="{Binding Categories[0].Properties[0].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[0].Properties[1]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[0].Properties[1].Id}" />
                                <Image Source="{Binding Categories[0].Properties[1].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[0].Properties[1].Values}"
                                         SelectedValue="{Binding Categories[0].Properties[1].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[0].Properties[2]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[0].Properties[2].Id}" />
                                <Image Source="{Binding Categories[0].Properties[2].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[0].Properties[2].Values}"
                                         SelectedValue="{Binding Categories[0].Properties[2].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[0].Properties[3]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[0].Properties[3].Id}" />
                                <Image Source="{Binding Categories[0].Properties[3].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[0].Properties[3].Values}"
                                         SelectedValue="{Binding Categories[0].Properties[3].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[0].Properties[4]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[0].Properties[4].Id}" />
                                <Image Source="{Binding Categories[0].Properties[4].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[0].Properties[4].Values}"
                                         SelectedValue="{Binding Categories[0].Properties[4].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>



                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[0].Properties[5]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[0].Properties[5].Id}" />
                                <Image Source="{Binding Categories[0].Properties[5].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[0].Properties[5].Values}"
                                         SelectedValue="{Binding Categories[0].Properties[5].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[0].Properties[6]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[0].Properties[6].Id}" />
                                <Image Source="{Binding Categories[0].Properties[6].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[0].Properties[6].Values}"
                                         SelectedValue="{Binding Categories[0].Properties[6].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[0].Properties[7]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[0].Properties[7].Id}" />
                                <Image Source="{Binding Categories[0].Properties[7].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[0].Properties[7].Values}"
                                         SelectedValue="{Binding Categories[0].Properties[7].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[0].Properties[8]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[0].Properties[8].Id}" />
                                <Image Source="{Binding Categories[0].Properties[8].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[0].Properties[8].Values}"
                                         SelectedValue="{Binding Categories[0].Properties[8].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[0].Properties[9]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[0].Properties[9].Id}" />
                                <Image Source="{Binding Categories[0].Properties[9].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[0].Properties[9].Values}"
                                         SelectedValue="{Binding Categories[0].Properties[9].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                    </WrapPanel>

                </Grid>
            </TabItem>

            <TabItem Header="{Binding Categories[1].Id}" IsSelected="True">
                <TabItem.Resources>
                    <Style BasedOn="{StaticResource GN.TextTabItemStyle}" TargetType="TabItem">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Categories[1]}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TabItem.Resources>

                <Grid Margin="10,0"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center">

                    <WrapPanel Width="{Binding Categories[1].Width}"
                               HorizontalAlignment="Center"
                               Orientation="Horizontal">

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[1].Properties[0]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[1].Properties[0].Id}" />
                                <Image Source="{Binding Categories[1].Properties[0].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[1].Properties[0].Values}"
                                         SelectedValue="{Binding Categories[1].Properties[0].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[1].Properties[1]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[1].Properties[1].Id}" />
                                <Image Source="{Binding Categories[1].Properties[1].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[1].Properties[1].Values}"
                                         SelectedValue="{Binding Categories[1].Properties[1].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[1].Properties[2]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[1].Properties[2].Id}" />
                                <Image Source="{Binding Categories[1].Properties[2].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[1].Properties[2].Values}"
                                         SelectedValue="{Binding Categories[1].Properties[2].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[1].Properties[3]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[1].Properties[3].Id}" />
                                <Image Source="{Binding Categories[1].Properties[3].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[1].Properties[3].Values}"
                                         SelectedValue="{Binding Categories[1].Properties[3].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[1].Properties[4]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[1].Properties[4].Id}" />
                                <Image Source="{Binding Categories[1].Properties[4].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[1].Properties[4].Values}"
                                         SelectedValue="{Binding Categories[1].Properties[4].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>



                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[1].Properties[5]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[1].Properties[5].Id}" />
                                <Image Source="{Binding Categories[1].Properties[5].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[1].Properties[5].Values}"
                                         SelectedValue="{Binding Categories[1].Properties[5].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[1].Properties[6]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[1].Properties[6].Id}" />
                                <Image Source="{Binding Categories[1].Properties[6].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[1].Properties[6].Values}"
                                         SelectedValue="{Binding Categories[1].Properties[6].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[1].Properties[7]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[1].Properties[7].Id}" />
                                <Image Source="{Binding Categories[1].Properties[7].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[1].Properties[7].Values}"
                                         SelectedValue="{Binding Categories[1].Properties[7].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[1].Properties[8]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[1].Properties[8].Id}" />
                                <Image Source="{Binding Categories[1].Properties[8].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[1].Properties[8].Values}"
                                         SelectedValue="{Binding Categories[1].Properties[8].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                        <Border>
                            <Border.Resources>
                                <Style BasedOn="{StaticResource PropertyBorder}" TargetType="Border">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Categories[1].Properties[9]}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Resources>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30" />
                                    <RowDefinition Height="120" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource PropertyTextBlock}" Text="{Binding Categories[1].Properties[9].Id}" />
                                <Image Source="{Binding Categories[1].Properties[9].Image}" Style="{StaticResource PropertyImage}" />
                                <ListBox Grid.Row="1"
                                         ItemsSource="{Binding Categories[1].Properties[9].Values}"
                                         SelectedValue="{Binding Categories[1].Properties[9].Selected}"
                                         Style="{StaticResource PropertyListBox}" />
                            </Grid>
                        </Border>

                    </WrapPanel>

                </Grid>
            </TabItem>

            <!--<TabItem Header="References" Style="{StaticResource GN.TextTabItemStyle}">
                <Grid Grid.Row="1" Margin="10,20">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="30" />
                    </Grid.RowDefinitions>
                    <Grid.Effect>
                        <DropShadowEffect BlurRadius="5"
                                          Direction="-90"
                                          Opacity="0.5"
                                          RenderingBias="Quality"
                                          ShadowDepth="2" />
                    </Grid.Effect>

                    <Grid Grid.Row="1" Background="{DynamicResource GN.Gray}">
                        <StackPanel Margin="20,0"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                            <TextBlock Style="{StaticResource IndicationStyle}" Text="Record: " />
                            <TextBlock Style="{StaticResource IndicationStyle}" Text="{Binding Indicator}" />
                        </StackPanel>

                        <TextBlock Margin="0,0,10,0"
                                   HorizontalAlignment="Right"
                                   Foreground="{Binding DataTypeColor}"
                                   Style="{StaticResource IndicationStyle}"
                                   Text="{Binding DataType}" />
                    </Grid>

                    <DataGrid ContextMenu="{StaticResource GridContextMenu}"
                              ItemsSource="{Binding ItemsCollection}"
                              SelectedIndex="{Binding SelectedIndex, Mode=OneWayToSource}"
                              SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                              Style="{StaticResource DataGridStyle}">
                        <DataGrid.Resources>
                            <Style BasedOn="{StaticResource RowsStyle}" TargetType="{x:Type DataGridRow}">
                                <Setter Property="ContextMenu" Value="{StaticResource GroupContextMenu}" />
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>

                            <DataGridTextColumn Width="150"
                                                Binding="{Binding ItemType}"
                                                ElementStyle="{StaticResource LeftColumn}"
                                                Header="Type" />

                            <DataGridTextColumn Width="250"
                                                Binding="{Binding Code}"
                                                ElementStyle="{StaticResource LeftColumn}"
                                                Header="Code" />

                            <DataGridTextColumn Width="400"
                                                Binding="{Binding Description}"
                                                ElementStyle="{StaticResource LeftColumn}"
                                                Header="Description" />

                            <DataGridTextColumn Width="80"
                                                Binding="{Binding Qty}"
                                                ElementStyle="{StaticResource CenterColumn}"
                                                Header="Qty" />

                            <DataGridTextColumn Width="151"
                                                Binding="{Binding Note}"
                                                ElementStyle="{StaticResource LeftColumn}"
                                                Header="Note" />

                        </DataGrid.Columns>
                    </DataGrid>

                    <Button Grid.Row="1"
                            Width="28"
                            Height="28"
                            Margin="0,0,5,0"
                            Padding="1"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource GN.CircuitButtonStyle}"
                            ToolTip="Refresh">
                        <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Images/Icons/Reload.png" />
                    </Button>

                    --><!--<c:LoadingDataControl Grid.RowSpan="2"
                                          DataContext="{Binding}"
                                          IsEnabled="{Binding IsRefreshing}"
                                          Style="{StaticResource LoadingControlTriggers}" />--><!--
                </Grid>
            </TabItem>-->



        </TabControl>
    </Grid>

</UserControl>
