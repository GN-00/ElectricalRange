<UserControl x:Class="ProjectsNow.Views.LibraryViews.SelectGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:ProjectsNow.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Width="600"
             Height="320"
             Background="{StaticResource GN.Gray}"
             BorderBrush="{StaticResource GN.GradientBlue}"
             BorderThickness="2"
             Style="{StaticResource ViewStyle}"
             mc:Ignorable="d">
    <UserControl.Resources>

        <Style x:Key="PropertyTextBlock" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Times New Roman" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Margin" Value="5,2,0,2" />
            <Setter Property="Width" Value="110" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="PropertyImage" TargetType="Image">
            <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Width" Value="20" />
            <Setter Property="Margin" Value="5,0,5,0" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="PropertyListBox" TargetType="ListBox">
            <Setter Property="Height" Value="110" />
            <Setter Property="Width" Value="130" />
            <Setter Property="BorderBrush" Value="White" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="Times New Roman" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ItemContainerStyle" Value="{DynamicResource ListBoxItemStyle}" />
        </Style>

        <Style x:Key="ListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Name="_Border"
                                Padding="2"
                                SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="_Border" Property="Background" Value="LightGray" />
                                <Setter Property="Foreground" Value="Red" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>

                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ListBoxItemGroupStyle" TargetType="ListBoxItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Name="_Border"
                                Padding="2"
                                SnapsToDevicePixels="true">
                            <ContentPresenter />
                            <Border.InputBindings>
                                <MouseBinding Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}" Gesture="LeftDoubleClick" />
                            </Border.InputBindings>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="_Border" Property="Background" Value="LightGray" />
                                <Setter Property="Foreground" Value="Red" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PropertyBorder" TargetType="Border">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Height" Value="155" />
            <Setter Property="Width" Value="150" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Background" Value="{StaticResource GN.Gray}" />
            <Setter Property="Effect" Value="{StaticResource GN.Shadow}" />
        </Style>

    </UserControl.Resources>
    <Grid Background="{StaticResource GN.Gray}">

        <c:PopupControls />

        <Button Width="100"
                Height="30"
                Margin="0,0,10,10"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Command="{Binding CancelCommand}"
                Style="{StaticResource CancelButton}" />

        <Button Width="100"
                Height="30"
                Margin="0,0,115,10"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Command="{Binding SaveCommand}"
                Style="{StaticResource GN.AcceptButton}" />

        <Button Width="100"
                Height="30"
                Margin="0,0,220,10"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Command="{Binding ResetSelectionsCommand}"
                RenderTransformOrigin="-0.208,0.784"
                Style="{StaticResource GN.ResetButton}" />


        <Grid Height="60"
              VerticalAlignment="Top"
              Background="{StaticResource GN.Gray}" />


        <TextBlock Style="{StaticResource TitleStyle}" Text="Groups" />


        <Grid Height="200"
              Margin="2,60,2,0"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Top"
              Background="White"
              Effect="{StaticResource GN.Shadow}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition Width="1.25*" />
            </Grid.ColumnDefinitions>


            <Border Style="{StaticResource PropertyBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="120" />
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource PropertyTextBlock}" Text="Manufacturers" />
                    <Image Source="{Binding ManufacturerImage}" Style="{StaticResource PropertyImage}" />
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding Manufacturers}"
                             SelectedValue="{Binding SelectedManufacturer}"
                             Style="{StaticResource PropertyListBox}" />
                </Grid>
            </Border>


            <Border Grid.Column="1" Style="{StaticResource PropertyBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="120" />
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource PropertyTextBlock}" Text="Families" />
                    <Image Source="{Binding FamilyImage}" Style="{StaticResource PropertyImage}" />
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding Families}"
                             SelectedValue="{Binding SelectedFamily}"
                             Style="{StaticResource PropertyListBox}" />
                </Grid>
            </Border>

            <Border Grid.Column="2"
                    Width="200"
                    Style="{StaticResource PropertyBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="120" />
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource PropertyTextBlock}" Text="Group" />
                    <Image Source="{Binding GroupImage}" Style="{StaticResource PropertyImage}" />
                    <ListBox Grid.Row="1"
                             Width="180"
                             DisplayMemberPath="Description"
                             ItemContainerStyle="{StaticResource ListBoxItemGroupStyle}"
                             ItemsSource="{Binding Groups}"
                             SelectedValue="{Binding SelectedId}"
                             SelectedValuePath="Id"
                             Style="{StaticResource PropertyListBox}" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

